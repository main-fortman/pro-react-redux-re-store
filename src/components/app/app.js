import React from 'react';
import ErrorIndicator from '../error-indicator';
import Spinner from '../spinner/';

export const App = () => {
    return (
        <div>
            <ErrorIndicator text='WOW!!!'/>
        </div>
    )
}
